function ret = stable(x,pow,const,limit, iter)
    ret = x;
    while(limit > 0)
        ret = ret .^ pow + const;
        if(abs(ret) > limit)
            ret = 0;
            break
        end
        iter = iter - 1;
    end
    ret = abs(ret - limit);
end