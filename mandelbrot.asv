f = @(x,c) x.^2 + c;
iteration = 10;
const = 1;

x_axis = linspace(-2,2,100);
y_axis = linspace(-2,2,100);

[X,Y] = meshgrid(-2:0.1:2 ,-2:.1:2); 
pixels = complex(X,Y);
pix
%Z = sqrt(X.^2 + Y.^2);
Z = stable(0,2,pixels,1,10);


scatter(X(:),Y(:),1,Z(:));

grid();
